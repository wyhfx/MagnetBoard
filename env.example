# 默认配置（已内置到docker-compose.yml中）
# 如需自定义配置，可以取消注释并修改以下内容

# 应用端口（默认17500）
# APP_PORT=17500

# 数据库密码（默认sehuatang123）
# DATABASE_PASSWORD=your_secure_password

# 代理配置（可选）
# PROXY_ENABLED=false
# PROXY_HOST=
# PROXY_PORT=
# PROXY_USERNAME=
# PROXY_PASSWORD=
